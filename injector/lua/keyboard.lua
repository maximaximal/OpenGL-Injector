local keyboard = {}

local function generateKeys()
    local keys = {
	
    }
    return keys
end

function keyboard.init()
    local kb = {
	keys = generateKeys()
    }
end

function keyboard.draw()
    
end

return keyboard
